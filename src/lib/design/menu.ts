export const DEFAULT_TEMPLATES = ["gradient_title", "bottom_bar", "corner_badge"] as const;

export const DEFAULT_STYLES = ["minimal_modern", "bold_tech"] as const;

export const DEFAULT_FORMATS = ["square", "portrait", "story"] as const;
