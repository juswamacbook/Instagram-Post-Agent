import type { DesignVariant } from "@/lib/design/types";

export async function renderVariantToPng(_variant: DesignVariant) {
  void _variant;
  // Stub for next phase: replace with renderer implementation.
  return {
    pngBuffer: null,
    width: null,
    height: null,
  };
}
